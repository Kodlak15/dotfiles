(include "../../widgets/hardware/cpu.yuck")
(include "../../widgets/hardware/ram.yuck")
;; (include "$HOME/.config/eww/widgets/hardware/ram.yuck")

(defwidget hardware []
  (eventbox
    :class "hardware-event"
    (box
      :class "hardware"
      (cpu)
      (ram)
    )
  )
)

