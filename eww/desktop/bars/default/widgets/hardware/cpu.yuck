(defwidget cpu []
  (eventbox
    :class "cpu-event"
    (box
      :class "cpu"
      (label
        :class "cpu-label"
        :text " "
      )
      {round(EWW_CPU["avg"], 1)}
      (label
        :text "GHz"
      )
    )
  )
)

